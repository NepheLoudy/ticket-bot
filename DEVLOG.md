# DEVLOG · ticket-bot（工单系统机器人）

版本隔离单位：一次 `npm run push`（= 一次 git 提交 + 一次部署）。v1~v42 于 2026-09-04 按提交历史回溯编号，此后每次 push 在文末追加新版本（规则见顶层 [AGENTS.md](../../AGENTS.md)）。

当前最新：**v63**（2026-09-12，随本提交落地）。

## 阶段十二 · 无人接单升级 + 结单提醒只私聊（2026-09-05）

### v50 · 2026-09-05 · 随本提交落地 · feat
**超时检查下辖分支：第 2 轮无人接单私聊对应组长 + 结单提醒移除转群兜底（只私聊）**
- **无人接单升级**（需求：群聊工单长期/第二轮提醒无人接单 → 回溯私聊本群对应的负责人）：`src/cron/index.js` 超时检查（每小时）处理工单时计轮次（`timeoutRoundState`）；**第 2 轮起**在常规分支动作之外私聊「面向组别」对应的组长（`nudgeGroupLeaders`）——多组别工单的多个组长各收一条，同一组长名下多组合并为一条；文案含组名/工单标题/超时小时数/接单指引（群内 @机器人 发「接单」或组长本人接单）/工单详情链接。同一工单对组长的私聊间隔不小于 **3 小时**（`leaderNudgeState`），避免每小时轰炸。
- 组长标识兼容：`GROUP_LEADERS` 值支持 open_id（`ou_` 开头直通）与 user_id（`/contact/v3/users/:id?user_id_type=user_id` 解析出 open_id，结果缓存；解析失败 fail-closed 跳过该组长并打告警）。当前 .env 配的是 user_id，已实测解析成功（机械组组长）。附带发现：超时重问询卡里 @组长 用的是卡片 `<at id>` 语法，配 user_id 时 @ 可能无效（LOGIC-MAP 已知限制 #10，该分支本身很少触达，暂未动）。
- **结单提醒改只私聊**（用户裁定：私聊链路已跑通，不需要「先私聊后转群」兜底）：`handleClosingTicket` 改为只私聊当前处理人一次即止（`closingRemindState` 去掉 groupNotified 状态机），删除群引导分支与 `buildCloseReminderCard`（`src/feishu/bot.js` 同步下线）；未结单工单的持续曝光由 pm-robot 每日 DDL 播报的「工单结单」分栏承担。全库排查确认这是最后一条私聊→转群链路（pm-robot 的 DDL 确认群聊降级已在 v51 删除）。
- 轮次/间隔/解析/提醒状态均在内存（48h 淘汰）：重启清零仅使升级推迟一轮或重私聊一次，不涉及写库故不落表；组长私聊先于当轮常规分支动作执行且自带 try/catch，失败不影响既有提醒。
- 文档：LOGIC-MAP.md §1.4 超时检查/结单提醒行重写 + 已知限制 #3/#8/#9 更新。

## 阶段十一 · 多组别漏播修复 + 多人接单（2026-09-05）

### v49 · 2026-09-05 · 随本提交落地 · feat
**多组别工单漏播修复（审批节点拆段匹配）+ 多人接单续接窗口 + 评审批修**
- **漏播根因修复**：并行审批流的「审批节点」字段会把同层多分支节点名以「；」拼接写入（5 组别工单=同值×5 段），整串精确匹配判为非触发节点 → 创建不播报、对账不补播，手动补播同门槛（202609050003 即此因）。新增 `config.matchNodeValue/splitNodeValues`（按 `;；,，、|` 拆段任一段命中即命中），替换 `isActivationNode`/`isAssignAcceptNode`/审批联动同意守卫/超时检查（超时检查改全量拉取后本地过滤）；存量漏播单「已播报」标记为空，部署后由对账 1 分钟内自动补播。
- **多人接单**（`config.multiAccept`，新 env `MULTI_ACCEPT_FIELD/MULTI_ACCEPT_YES_VALUE/MULTI_ACCEPT_WINDOW_HOURS/MULTI_ACCEPT_WINDOW_FIELD`，见 .env.example）：工单表「是否允许多人接单」=是 时——接单**不即时通过审批**；写「多人接单截止」=now+6h（工单级计时器，多组别共享；字段自动创建，跨重启恢复）；向已有人接单的群（各接单人组别群 ∪ 接单发生群）发「👥 多人接单进行中」卡（人数/名单/截止/续接方式）；窗口内再有人接单 → 合并补充负责人 + 重置计时 + 再次通告；到期由每分钟对账自动通过全部触发节点任务并向已接单的群发「✅ 多人接单结束」卡（10 分钟节流，失败下轮重试）。补充负责人接单时改**合并写入**（原第二人覆盖第一人）。指定负责人工单不走多人分支。**上线前提**：审批表单→表格同步自动化需映射「是否允许多人接单」（列已在，近期记录值为空；映射前一律按"否"走现状）。
- **审批联动批量通过**（`approvalLinkService.js`）：待审任务缓存改 `{申请编号 → {taskId → 任务}}`，`autoApproveForTicket` 逐个通过该实例**全部**待审任务（并行分支全过流程才汇合流转；单任务场景行为不变）；对账新增非多人单「接单后审批补通过」补偿（指定负责人工单不代通过，公示即绑定必须等本人确认）。
- **评审批修**：接单确认改整句精确匹配（去空白后等于「接单/确认接单」；含「接单」但非整句如"还没人接单吗""我不想接单"只回提示不触发，防误接单/误自动通过）；接单回退池仅收触发节点且命中候选全部升序入列（原只入一条且含回执单节点，重启后 stray"接单"会错配已接单工单）；超时分支文案按"无人接单"语义修正（分支1 不再引导发起人去"结单"）、补充负责人/是否指定/面向组别改走 config、发起时间统一 `getCreatedTime`（发起时间→创建时间回退）；播报开关收敛 `isBroadcastEnabled()`（create/update 共用语义注释）。
- 新增诊断脚本：`scripts/probe-latest-nodes.js`（最新记录+审批节点分布）、`probe-fields.js`（列名+原始值）、`probe-ticket.js`（按申请编号定位+播报判定模拟）。
- 附带：工作区归拢（ticket-pm/）后的 AGENTS.md/DEVLOG.md 相对路径修正随本批提交。

## 阶段十 · 24h 追问与私聊确认（2026-09-05）

### v48 · 2026-09-05 · 随本提交落地 · feat
**指定负责人 24h 未确认私聊追问 + 私聊回「接单」确认链路；DDL 分桶多人名字拼接（修3）**
- 用户裁定补充：公示即绑定后超 24h 无人确认 → 私聊负责人追问「是否知道工单」，其私聊回复「接单」与群内 @机器人 接单等效，共同补充「负责人确认消息后通过」节点的通过判定；管理层群只同步工单发布与问询（DDL 分栏维持不含管理层，hub 仅读四个播报群键）；DDL 卡片负责人多人用「、」拼接。
- `src/cron/index.js`：新增每小时「确认追问」任务（`runAssigneeNudgeCheck`）——节点仍在「负责人确认消息后通过」+ 已绑定（补充负责人==指定负责人）+ 距发起超 `ASSIGN_NUDGE_HOURS`（默认 24h）→ 私聊负责人；同工单追问间隔不小于该时长，状态 7 天淘汰；`/api/bot/test-nudge` 手动触发。
- `src/services/ticketService.js`：新增 `handleAssigneeDmConfirm`——按发送者定位「已绑定未确认」工单，登记到公示群待接单映射后复用 `handleAcceptOrder`（本人校验/状态推进/群内回执/审批联动一条链）。
- `src/services/chatService.js`：新增 p2p+接单 分支（网关 v11 路由配套）；`message_id` 去重（防网关重启窗口/飞书重投导致接单重复处理）。
- `src/services/unclosedService.js`：handlerName 多人拼接；注释写明 DDL 口径（指定→补充负责人，不取当前处理人）与管理层/未匹配组别不进分栏的设计。
- 联动：feishu-gateway v11（chatType 匹配器 + p2p 接单路由 + 路由日志补 chat_id）同批部署。
- 排查备忘：机械组接单漏回执=旧卡片引导 @自动型（webhook 无事件）+ v47 删除回扫的过渡期现象，非新链路 bug；应用机器人须在 6 个组别群内（`im:chat:readonly` 权限未开通，暂无法 API 探测成员身份）。

## 阶段九 · 指定负责人链路闭环（2026-09-05）

### v47 · 2026-09-05 · 随本提交落地 · feat
**播报与接单回执回归对话型应用机器人（删除每分钟消息回扫）+ 指定负责人「公示即绑定」**
- 用户裁定：每分钟回扫群消息太耗费算力；webhook 自定义机器人收不到事件是回扫存在的根源，卡片按钮回调是应用级配置（五机器人共用一个应用会互相干扰）——故播报与接单触发整体回归对话型（架构铁律钦定的工单域例外，v38 前的原路）。
- `src/feishu/bot.js`：`sendCardToTarget` 改 chat_id 优先（应用机器人 IM API），失败回退 webhook；接单指引统一改「在群内 @爆米花机-对话型 并发送『接单』」。**要求应用机器人在各组别群内**。
- `src/services/chatService.js`：重开事件驱动接单——网关路由（含「接单」且 @机器人 → ticket-bot）转来的事件中，群聊 + 二次校验 @本应用机器人（`isSelfMention`：app/self/bot+名称多信号）→ `handleAcceptOrder`；失败原因以文本回复到群。
- `src/services/ticketService.js`：删除 `backfillAccepts`/`listBotMentions`（每分钟 IM 消息列表轮询下线）；新增 `bindAssignedTicket`「公示即绑定」——指定负责人工单播报成功后仅写补充负责人 + 按组别合并看板人员字段，**状态推进与审批自动通过仍由本人 @机器人 确认触发**（用户明确裁定）；对账循环增加绑定补偿（已播报未绑定的补绑定，计数 `bound`）；接单回退查询放行「已绑定未确认」（补充负责人==指定负责人）工单。
- `src/config.js`：新增 `approvalNode.assignAcceptValue`（`APPROVAL_NODE_ASSIGN_ACCEPT_VALUE`，默认「负责人确认消息后通过」）；删除 `broadcast.acceptBotName`（`ACCEPT_BOT_NAME` 下线）。
- 文档：README 播报/接单章节与架构备注、`.env.example`、AGENTS.md 交互契约行同步。
- 注意：**应用机器人必须在组别群内**，否则该群卡片走 webhook 兜底且接单事件不可达（日志有回退告警）。

### v46 · 2026-09-05 · 随本提交落地 · fix
**审批联动缓存缺失兜底落地：按申请编号反查实例列表定位待审任务（存量工单/重启后也能自动通过）**
- v40 留下的「缓存缺失兜底」只有注释没有实现（缓存 miss 直接返回失败）——v45 部署重启后，已在触发节点待审的存量工单缓存为空，接单时审批联动会落空。
- `src/services/approvalLinkService.js` 新增 `findPendingTaskByApplicationNo`：缓存 miss 时按 `APPROVAL_CODE` 反查近 14 天实例列表（POST /approval/v4/instances），逐实例比对表单「申请编号」，定位白名单审批人名下的 PENDING 任务并回填缓存后自动通过；仍找不到才返回待重试。同意层审批节点守卫先行，反查不会触碰回执单等其它节点。

### v45 · 2026-09-05 · 随本提交落地 · feat
**指定负责人工单：通讯录查组别群内公示 + 本人 @机器人 确认 + 机器代理通过「负责人确认消息后通过」（白名单管理员）**
- 参照无指定负责人代理审批监听链路（v38~v42），把「有指定负责人」分支补成同款闭环。
- `src/services/ticketService.js`：指定负责人分支播报后同样登记待接单映射（`expectedAssigneeId` 绑定本人）；接单确认三路径（内存/回退/回扫补录）加本人校验——指定负责人工单仅限本人 @爆米花机_自动型 确认，他人 @ 忽略；回退匹配补「负责人组别求交」（播报群来自负责人组别解析，可能不在面向组别）；回扫补录按负责人组别定位播报群；确认时重查源表为准（授权门禁，拉取失败按拒绝等下轮回扫重试）。
- `src/services/approvalLinkService.js`：联动扩为双分支——未指定负责人→「群内有组员接单后通过」（组员确认即通过），指定负责人（本人确认）→「负责人确认消息后通过」；审批人白名单支持单值+多值（`APPROVAL_AUTO_APPROVER_IDS` 取并集），名单外任务到达打印审批人 open_id 留痕；同意前新增审批节点守卫（工单须仍在触发节点，防实例推进到回执单后误自动通过）；审批意见区分 组员/负责人。
- `src/feishu/bot.js`：指定负责人公示卡片加「接单方式：仅限本人 @机器人 确认接单」指引。
- `.env`：激活 `APPROVAL_AUTO_APPROVER_ID`（白名单管理员本人，取自私聊白名单管理员 open_id；若实际审批人不符，按「跳过非联动审批人」日志修正）。
- 踩坑：接单回退匹配原把工单 push 进未挂回 Map 的临时数组，仅当次调用生效——已挂回持久化；事件不带审批节点名，联动靠「审批人白名单（缓存层）+ 工单审批节点校验（同意层）」双层防误同。

## 阶段八 · 指令边界收紧（2026-09-05）

### v44 · 2026-09-05 · docs（随本提交落地，无独立哈希） · feat
**指令仅群内触发；私聊指令仅管理员白名单可用（与 hub 同套规则）**
- `src/services/chatService.js`：/ticket-* 指令在非群聊会话命中白名单（`P2P_COMMAND_OPEN_IDS`/`P2P_COMMAND_CHAT_IDS`，open_id 与 p2p chat_id 任一命中）才执行，其余私聊指令回复「仅支持在群聊中使用」；群内指令照旧回复到来源群。
- `src/config.js` 新增 `p2pCommandAllow` 白名单配置；`.env.example` 补充说明。本地 .env 已配置管理员 open_id。
- 同批补交 v43 建档条目（昨日「当前最新」头部与阶段七段落遗留未提交）。

## 阶段七 · 开发历史建档（2026-09-04）

### v43 · 2026-09-04 · `918a2d5` · docs
**新增 DEVLOG 开发历史 v1~v42（按 push 回溯建档）**
- 本 DEVLOG 诞生：v1~v42 按提交历史回溯编号，此后每次 push 追加一版（规则见顶层 AGENTS.md「开发日志（DEVLOG）」节）。

## 阶段一 · 诞生与部署链路磨合（2026-08-28）

### v1 · 2026-08-28 · `0722202` · init
**init: ticket-bot - 工单系统机器人，含多维表格事件订阅、category门控搬运、群聊分支播报、接单确认与超时提醒**
- 首提即带完整骨架：`src/feishu/`（client/bot/bitable/eventSubscription）、`src/services/`（chat/sync/ticket）、cron、5 个 scripts 探针脚本，CI + Actions 部署俱全。

### v2 · 2026-08-28 · `1d2f251` · feat
**添加一键部署脚本 push.js（提交到 GitHub 并部署到 NAS）**
- `npm run push` 链路首次成形，此后所有版本以此为隔离单位。

### v3 · 2026-08-28 · `f54aa40` · fix
**部署改用 SSH 协议访问 GitHub（NAS 443 端口不通）**

### v4 · 2026-08-28 · `cfa1f90` · feat
**添加 SFTP 直传部署脚本（本地无法访问 GitHub 时绕过部署）**

### v5 · 2026-08-28 · `eeaad1e` · feat
**统一部署脚本（代码进git+配置进NAS+部署一步完成）**
- push.js 收敛为唯一部署入口，deploy-sftp 等被吸收。

## 阶段二 · 多维表格事件与播报联调（2026-08-29）

### v6 · 2026-08-29 · `7d3ff76` · fix
**修正多维表格事件名 drive.file.bitable_record_changed_v1 并添加云文档事件订阅**
- 附带临时排查脚本 diag.js（v7 即删）。

### v7 · 2026-08-29 · `f77835e` · chore
**移除临时排查脚本 diag.js**

### v8 · 2026-08-29 · `8dc1b88` · fix
**修正多维表格事件体解析（action_list 结构）**

### v9 · 2026-08-29 · `9672b77` · fix
**播报时机改为申请状态进入审批中时触发（新建/更新均支持）**

### v10 · 2026-08-29 · `f1acdc2` · fix
**修复卡片 elements 元素格式（markdown 对象而非字符串）**
- 引入 trigger.js 手动触发调试。

### v11 · 2026-08-29 · `4f6beff` · feat
**监听审批节点、结单提醒、卡片文案/字段/日期优化**

### v12 · 2026-08-29 · `093bac7` · feat
**接单确认写入补充负责人字段**

### v13 · 2026-08-29 · `0a0ada1` · docs
**更新 README 与 .env.example 至当前逻辑**

## 阶段三 · 对账兜底与看板同步（2026-08-31）

### v14 · 2026-08-31 · `2e11304` · fix
**指定负责人工单播报支持「负责人确认消息后通过」节点，新增补播接口**

### v15 · 2026-08-31 · `864c8de` · feat
**播报对账兜底（每分钟扫描触发节点工单补播/补搬运，源表写已播报标记防重播）**
- 可靠性里程碑：漏播不再依赖人工发现，cron 对账自动补。

### v16 · 2026-08-31 · `2f3ebe8` · fix
**搬运字段缺失自动建字段，对账搬运不依赖播报标记**

### v17 · 2026-08-31 · `e0c1b3a` · docs
**目标表源记录ID字段自动创建说明**

### v18 · 2026-08-31 · `7f36066` · style
**统一两条播报链路卡片配色与图标（蓝色📬新工单提醒，以指定负责人卡片为准）**

### v19 · 2026-08-31 · `9ae61ea` · feat
**看板人员字段按人员实际组别映射（负责人/接单人），抽取公共组别解析模块**
- `src/utils/personFields.js` 诞生，后被 DDL 分桶、看板搬运等多处复用。

### v20 · 2026-08-31 · `3b85381` · feat
**看板子项目 name 统一为（category支持项目）格式，通讯录部门名已可读**

### v21 · 2026-08-31 · `8d3c481` · feat
**看板 status 状态机（已通过→completed，等待接单/确认→waiting，回执单→in_progress），upsert 防倒退**

### v22 · 2026-08-31 · `62473c5` · fix
**对账支持回执单节点状态推进，接单确认内存映射空时回退查源表**

### v23 · 2026-08-31 · `5da065a` · fix
**接单确认回退查询支持回执单节点工单（补录接单人）**

### v24 · 2026-08-31 · `0eccf4c` · feat
**对账回扫群@消息补录被分流的接单，播报前重查防已接单重复问询**

### v25 · 2026-08-31 · `0e5c045` · fix
**审批群消息不再抢接单——新增 IGNORE_CHAT_IDS 忽略群配置**

## 阶段四 · 接入统一网关（2026-09-01）

### v26 · 2026-09-01 · `f444852` · fix
**结单提醒/填写引导改用申请编号自带的审批实例链接**

### v27 · 2026-09-01 · `90331ef` · feat
**接入统一事件网关改为 HTTP 消费；部署脚本与密钥分离（push.js 统一入口）**
- 架构转折点：自有长连接下线，改为 feishu-gateway 的 CONSUMERS；deploy*.js 全删。

### v28 · 2026-09-01 · `226ec9e` · feat
**结单提醒改为理想结单时间过后1天触发，文案同步调整**

### v29 · 2026-09-01 · `13747b5` · fix
**push.js 增加 NAS git fetch 失败自动转 SFTP 直传的兜底**

### v30 · 2026-09-01 · `2999472` · feat
**新增未结单工单按负责人组别分桶 API（供 DDL 播报分组分栏）**
- `src/services/unclosedService.js` 新增，/api/tickets/unclosed-by-group 成为 pm-robot DDL 卡片的数据源。

### v31 · 2026-09-01 · `7109f60` · fix
**补充 dayjs 依赖（未结单分组服务需要）**
- 教训：新依赖必须随代码同次 push，否则 NAS 起不来又挂。

### v32 · 2026-09-01 · `6c4ca40` · fix
**移除 Actions 旧式部署（Secrets 回写 .env 与 push.js 冲突），统一走 push.js**

## 阶段五 · 财务周播报误发与回退、边界文档（2026-09-02 ~ 09-03）

### v33 · 2026-09-02 · `b442876` · feat
**播报改纯定时，新增财务周播报（催发票/制单/转账），统计改本周；修复已接单仍超时播报与接单回执无姓名**
- 财务周播报属误发需求（本域归 approval-bot）。

### v34 · 2026-09-02 · `aa53d94` · revert
**Revert v33 全部改动**

### v35 · 2026-09-02 · `e9aa76f` · revert
**revert: 财务周播报为误发需求整体回退，保留已接单跳过超时播报与接单回执姓名解析两个修复**
- 净效果 = v33 − 财务域内容。

### v36 · 2026-09-02 · `853b6a3` · docs
**增加项目职能边界声明（发错会话防护，需求错位即提醒停手）**

### v37 · 2026-09-03 · `4ee5941` · docs
**AGENTS.md 增加「顶层规则与交互性」段（独立会话内联交互契约，开工先读顶层总表）**

## 阶段六 · 接单精准化与审批联动（2026-09-04）

### v38 · 2026-09-04 · `84f85b2` · feat
**接单监听改为回扫匹配@爆米花机_自动型（mention结构匹配），对话型不再触发接单**
- 与对话型机器人（hub）彻底解耦，接单只认自动型 @。

### v39 · 2026-09-04 · `1947770` · chore
**审批节点「有组员接单后通过」更名为「群内有组员接单后通过」，新旧值并存兼容存量工单**

### v40 · 2026-09-04 · `ce7a1ef` · fix
**接单回退多组别匹配 bug（电控/硬件共群互踩）+ dkyj 人员字段合并语义防对账覆盖 + 审批联动（接单自动通过）+ body 2mb**
- `src/services/approvalLinkService.js` 诞生：接单确认自动通过对应审批任务。

### v41 · 2026-09-04 · `4b33fd2` · feat
**配置工单审批 code（8A028329）+ 联动空值守卫（未配审批人时安全关闭）**

### v42 · 2026-09-04 · `b436a35` · feat
**审批联动未激活态记录任务审批人（用于 open_id 配置激活）**

---

**备注**：顶层 monorepo 中还留有一份本项目的旧快照副本（2026-08-31 `5e61ac3` 归档时点），以本仓库为准；顶层副本的滞后不影响部署（部署走本仓库 push.js）。

## 阶段七 · 全项目审查修复批次（2026-09-06）

### v50 · 2026-09-06 · 随本提交落地 · fix
**全项目审查修复：审批联动申请编号口径 + 接单确认守卫 + 拆段匹配统一 + 文档对齐**
- approvalLinkService：申请编号为超链接字段（{text,link}）时被 formatFieldValue 渲染成 `[text](link)` markdown，与审批表单里的纯文本编号永不匹配——缓存不命中、反查不命中，接单自动通过/对账补通过全链路有失效风险；改 formatFieldText 取纯文本（纯文本字段同样兼容）。
- 接单确认补三道守卫（以源表最新状态为准）：审批节点已推进→拒绝、本人已确认过→拒绝、非多人单已被他人接单→拒绝——堵跨群陈旧条目产生虚假接单回执/重复写表的洞；registerPendingOrders 幂等（播报复试/补播不再产生重复登记）。
- syncService mapStatus 改 matchNodeValue 拆段匹配（并行分支「；」拼接节点判不出 waiting/in_progress，v49 整改漏改处）；cron 24h 追问同步改为全量拉取+拆段匹配（原服务端等值过滤在拼接值下静默失效），发起时间缺失/非数值跳过本轮（原 NaN 比较恒 false 会误判超时立即追问）。
- DEFAULT_CHAT_ID 支持裸 chat_id / webhook:URL（原 parseRouteTargets 要求「名字=」格式，裸值被静默丢弃致兜底群永久失效）；isSelfMention 放宽为 mentioned_type=app/bot 即命中（群内机器人显示名与配置名不一致时接单不再被静默丢弃，与 pm-robot 同款）。
- 文档对齐：README 删失效命令（deploy:check/deploy:config）与幽灵 deploy 脚本、补 /api/bot/test-nudge 与 9 个脚本说明、配置表补 MULTI_ACCEPT_*/ASSIGN_NUDGE_HOURS/APPROVAL_NODE_ASSIGN_ACCEPT_VALUE；.env.example 补 FIELD_MAPPING/WATCHED_FIELDS/IGNORE_CHAT_IDS 占位；scripts/verify-nas.js（上轮漏播排查工具）随批入库。
- 版本线备注：v43~v49 期间条目未及时入档，版本号以 git 提交消息为准（9dcbc8d=v49），本条起恢复逐 push 记录。

## 阶段十三 · 晚间静默——播报时段限制（2026-09-06）

### v51 · 2026-09-06 · 随本提交落地 · feat
**02:00–09:00（Asia/Shanghai）静默窗口：定时/自动播报积压到 09:00 统一补发（可配可关）**
- 新增 `src/utils/quietHours.js` 通用闸门（顶层 AGENTS.md「晚间静默」规则的本仓实现）：窗口 `[QUIET_HOURS_START, QUIET_HOURS_END)`（默认 2→9，支持跨午夜写法，`QUIET_HOURS_DISABLED=1` 关闭）内播报不直接发送，积压持久化到 `.quiet-backlog.json`（重启不丢），启动时过点立即补冲刷、未过点调度到 09:00；冲刷失败单条保留重试 ≤3 次。
- 按任务形态接线（挤压要为挤压之后的事情负责）：①超时检查/结单提醒/确认追问（每小时）静默内**整轮跳过**——不计轮次、不写提醒/节流状态，09:00 整点轮次天然就是冲刷；②工单播报 `broadcastTicket` 静默内**直接顺延且零副作用**（不发送/不写播报标记/不公示即绑定/不登记待接单），每分钟对账在 09:00 后第一个 tick 自然补播，播报前重查兜住夜间已接单/节点推进；③每日汇总 `gateTask` 按触发槽位登记、冲刷重跑（取补发时刻数据）；④多人单结束通告 `gatePayload` 原样落盘卡片载荷按序补发（审批自动通过本身不延迟，只延后通告）。
- 豁免：对话/指令回复（接单确认等交互回路）与人工当下主动触发（`/api/bot/rebroadcast` 单条补播 `bypassQuiet=true`、`/test-*` 手动接口）。
- 其他：`/api/bot/cron-status` 附 `quietHours` 状态（窗口/是否静默/积压条数/下次冲刷）；`.quiet-backlog.json` 进 .gitignore；.env.example 补 `QUIET_HOURS_*` 占位。
- 版本线备注：v50 在历史中出现两次（`0607a21` fix 审查修复批次 + `28efc2b` feat 无人接单升级），历史条目不改写，本条起自 v51 续增。
- 文档：ticket-pm/LOGIC-MAP.md §1.4 注 + 新增 §1.7；顶层 AGENTS.md 新增「晚间静默」规则段。

## 阶段十四 · 群内问询节流 + DDL「无人接单」分栏（2026-09-06）

### v52 · 2026-09-06 · 随本提交落地 · feat
**无负责人工单群内问询改 6h×2 封顶 + unclosed API 新增无人接单分桶（v51 晚间静默同批上线）**
- 超时分支 2.2（无指定负责人）的群内重问询从「每小时一次、无上限」改为**每 6 小时一次、每单封顶 2 次**（≈发起后 6h、12h 各问询一次）：新增 `TIMEOUT_REASK` + `reaskState`（内存计数，重启清零、48h TTL，与轮次状态同款取舍）；至少一群发送成功才占用额度（全失败不消耗，下轮重试）；间隔未到/已达封顶的轮次只跳过群内问询卡——第 2 轮起的「无人接单升级」组长私聊照常（≥3h 间隔），封顶后由它承担持续提醒。**多人单不受限**：有人接单即合并写补充负责人、天然退出本检查；续接窗口的续接询问（ticketService 接单路径）与到期自动通过（对账路径）不经此处。
- `unclosedService.getUnclosedByGroup` 新增 `unclaimed` 分桶：节点 ∈ 触发节点（拆段匹配）+ 补充负责人为空 + 距发起 ≥6h（与超时检查阈值对齐，刚发布的不曝光）；无负责人可解析，按工单「面向组别」直连 `GROUP_ROUTES` 映射播报群；按已发布时长降序。返回结构 additive（每 chatId 增加一键，pm-robot 旧读法兼容），供 pm-robot DDL 卡「无人接单」分栏取数。
- 顺带修复：结单分桶原走服务端等值过滤（`审批节点 = "回执单：是否结单"`），并行分支「；」拼接节点值匹配不上会**静默漏桶**——随本批改全量拉取 + `matchNodeValue` 拆段匹配（与超时检查/确认追问 v50 同款整改）。
- 文档：ticket-pm/LOGIC-MAP.md §1.4（超时检查行）/§1.6（API 口径）、pm-robot 侧 §2.2(4)；顶层 DEVLOG v31 联动摘要。

## 阶段十五 · 例行维护纠偏（2026-09-06）

### v53 · 2026-09-06 · 随本提交落地 · fix
**全仓例行 debug 扫描——「；」拼接拆段匹配残留 2 处整改 + 状态文案修正 + README 对齐 v50~v52**
- `checkClosingTickets`（结单提醒）原走服务端等值过滤 `审批节点 = "回执单：是否结单"`：并行分支拼接节点值永不相等，**多组别工单的结单私聊永不触发**——改全量拉取 + `matchNodeValue` 拆段匹配（与确认追问 v50 / unclosed 结单分桶 v52 同款整改的最后一处）。
- 播报对账（ticketService）「回执单」节点判定原为整串精确比对 `node === closeValue`，多段拼接的结单工单每分钟对账不做补搬运/状态推进——改 `config.matchNodeValue` 拆段匹配（mapStatus v50 已改，此处闸门漏改）。
- `/api/bot/cron-status` 结单提醒展示文案「提前 N 天提醒结单」与实际语义（理想结单时间**过后** N 天）相反，修正。
- README 对齐：§4 超时处理补 v50 组长私聊升级 / v52 问询 6h×2 封顶 / 补充负责人非空跳过守卫、分支1 改「无人接单」文案口径；§5 结单提醒删已下线的转群兜底（v50，仅私聊一次，持续曝光由 pm-robot DDL 分栏承担）；§3 补「晚间静默」配置节（QUIET_HOURS_*）；§五 API 表补 `GET /api/tickets/unclosed-by-group`；§九 结构树补 `src/utils/quietHours.js`。

## 阶段十六 · 接单阻断修复 + 续接询问收窄（2026-09-06）

### v54 · 2026-09-06 · 2df1b23 · fix
**有人接单却仍在超时播报——接单主流程被看板缺失阻断（recvulRfoRHhsI 事故）+ 多人单续接询问仅发接单群**
- 事故链路：群内 @机器人 发「接单」→ `handleAcceptOrder` 第 1 步 `syncService.updateProjectStatus`（写看板 in_progress）在项目看板查不到对应记录，`findTargetRecordByKey` 落空后 throw「未找到对应项目记录」→ 被外层 catch 整体吞掉、按接单失败返回（群里只回 `⚠️ 未找到对应项目记录: recvulRfoRHhsI`）→ **补充负责人没写、确认卡/看板人员字段/审批联动/多人窗口全部跳过**；而每小时超时检查只认「补充负责人非空」跳过（cron §超时检查首道守卫），该单于是在全部面向组别群继续超时问询+组长私聊。看板记录为何缺失：搬运走 category 门控（`CATEGORY_FIELD=category`），category 为空的工单本来就不进看板，而对账的补搬运同样过这道门——看板是派生视图，不该做接单的前置条件。
- 修复①：`handleAcceptOrder` 看板状态更新改 try/catch 降级（失败仅打警告，不阻断），接单事实源回归源表（补充负责人 + 审批节点）；看板人员字段写入本就是缺失即跳过的非致命路径，不动。
- 修复②（用户裁定）：多人单续接询问卡（「⏳ 开放续接至 xx」）从「各接单人组别群 ∪ 本次接单群」改为**只发本次接单发生的群**——原实现里 `resolvePersonGroups` 解析失败会回退到工单「面向组别」，等于把"还有要接单吗"广播给全部组；窗口结束通告仍走 `collectMultiNoticeTargets`（收尾知会，保留组别口径），该函数注释同步收窄。
- 部署后补救：事故单 recvulRfoRHhsI 当时接单中止、无任何副作用（补充负责人为空、节点未动），在原群重新 @机器人 发「接单」即可正常完成；补充负责人写上后超时播报自然停止。
- 文档：README §4 接单确认链路补一句「看板状态更新失败不阻断接单」。

### v55 · 2026-09-06 · 89d23c8 · fix
**审批联动上线以来从未成功过的五连 bug 全修（接单后审批节点滞留触发节点的根因）**
- 背景：v54 部署后 recvulRfoRHhsI 重新接单成功（17:48，补充负责人已写、超时播报全停），但审批节点滞留「群内有组员接单后通过」，error 日志每 10 分钟刷 `查询审批实例列表失败: field validation failed (99992402)`。回溯日志确认审批联动两条链路（事件缓存 + 反查兜底）从未成功过——「接单→自动通过审批」一直是空转，此前工单的审批都是人工过的。
- NAS 直连探针逐参数二分，定位五个独立问题（`src/services/approvalLinkService.js`）：
  ① `getInstanceDetail` 硬编码 `locale=zh_cn`（下划线非法）→ 详情接口恒 99992402；合法写法是 `zh-CN` 或不传，删掉。响应体实例对象在 `data` 顶层（`task_list/form` 直接挂在 data 上），原代码取 `data.instance` 恒 undefined → 即使去掉 locale 也全链路 no-op，改为 `data.instance || data` 兜底。
  ② 反查用的旧「批量获取实例ID」`POST /approval/v4/instances` 对本应用恒 99992402（毫秒/秒时间戳、offset、page_size、去 user_id_type 全试过都一样），换实例搜索接口 `POST /approval/v4/instances/query`，且 `approval_code` 必须传字符串——传数组报 `9499 Invalid parameter type`。
  ③ 详情接口 `form` 是 **JSON 字符串**，`extractLinkInfo` 直接 `for...of` 迭代拿到的是单个字符，申请编号永远匹配不上 → 事件路径的缓存 key 恒空；先 `JSON.parse` 再迭代。
  ④ `task_list` 审批人是 **user_id 格式**（如 `778a737g`，`user_id_type=open_id` 也不改变返回），白名单 `APPROVAL_AUTO_APPROVER_ID` 配的是 open_id → 永不相等被 fail-closed 跳过，且 approve 接口按 `user_id_type=open_id` 调用传 user_id 必错。新增 `resolveToOpenId`（通讯录 user_id→open_id 解析，带缓存，同 v50 组长解析模式），事件缓存与反查兜底两路入库前统一归一。
  ⑤ 匹配口径改按搜索接口返回的 `serial_id`（审批编号 == 工单表申请编号），不再逐实例拉详情解析表单，反查从 O(n) 详情请求降为 1 次详情。
- 实测验证（NAS 探针全链路）：真实实例 `81AFF507-…`（202609050003）detail code=0，4 个并行 PENDING 任务审批人 `778a737g` 解析后命中白名单 `ou_2499…`；另确认工单卡 applink 里的 `instanceId=7682036685…` **不是** API 实例 code（detail 报 1390003），不能用于详情/同意调用。
- 部署后动作：重启清掉 10 分钟节流，对账兜底会对 202609050003 反查定位 4 个待审任务并批量同意（同意接口是全链路唯一未实测步骤，结果随部署验证记录）。

### v56 · 2026-09-06 · b8bad0f · docs
**v55 部署验证结果记录（docs，无代码改动）**
- 202609050003 审批联动闭环确认：手动触发对账后日志 `缓存缺失，已按申请编号反查定位 4 个待审任务` → `已自动通过审批: 202609050003（4/4 个任务）`；审批实例 timeline 四条 PASS 带批注「接单人已在群内确认接单，审批自动通过（对账补偿）」，并行分支汇合流转，**回执单节点的任务已生成**待发起人处理——同意接口（`POST /approval/v4/tasks/approve`）实测通过，v55 五处修复全链路生效。
- 源表「审批节点」字段由审批侧的同步自动化回写，同意成功数分钟后暂未刷新——工单侧无依赖（补充负责人非空已跳过超时检查；节点同步到「回执单」后该单自然进 DDL 结单分栏，由杨彬意作为接单人曝光），留意其自动刷新即可，非本仓问题。
- 附带确认：每分钟对账的「审批补通过」补偿（v49 引入、因本批 bug 一直空转）现已真正生效，存量「已接单但审批滞留触发节点」的工单会被逐分钟对账自动补通过（本次扫描 36 条仅 202609050003 一条符合，无积压）。

## 阶段十七 · 接单排队——同群多张待接单工单的序号区分（2026-09-08）

### v57 · 2026-09-08 · 2f464ea · feat
**同群多张待接单工单时接单监听撞车——按序号「接单N」排队区分 + 已发卡片接单词自动改写**
- 问题：一个群内同时存在多张「等待接单关键词」的工单（无人接单、多人单等待续接、指定负责人待本人确认）时，群里有人 @机器人 发「接单」固定命中最新一张（旧实现：内存映射从队尾取最新、非本人单静默顺延），无法表达接哪张、老单永远排不上——撞听。
- 排队规则（按用户裁定）：群内**只有一张**待接单工单 → 提示「接单」；**复数张** → 各工单卡片提示「接单1」「接单2」…（**最新播报的为「接单1」**，与队列头一致），发「接单N/确认接单N」指定接单，复数张时发裸「接单」会被拒并提示序号范围（不再静默错接）；有人接单或多人单窗口结束、工单离开队列后，剩余工单序号前移（接单2→接单1），仅剩一张时回落「接单」——**跟排队一样**；指定负责人的排队只对本人生效，且私聊确认（p2p+接单）不受群内排队影响（显式指定工单 ID，不用序号词）。
- 实现（`src/services/ticketService.js`）：
  - 内存待接单映射（`pendingOrdersByChat`/回退拉表补池）整体删除，改为**按源表数据实时推导各群队列** `computeAcceptQueues`——候选口径与旧回退查询完全一致（触发节点拆段匹配 + 无人接单/指定即绑定未确认/多人单续接窗口内），按创建时间倒序（最新=「接单1」）；序号不落库，跨重启稳定一致，天然兼容对账补播后入队（补播走 broadcastTicket 同一入口自动带序号）；
  - 接单关键词精确匹配扩为 `接单|接单N|确认接单|确认接单N`（chatService），序号超范围/不存在时明确提示当前数量；
  - 队列变化（新播报入队/接单出队/多人单窗口结束/节点推进）后**自动改写已发卡片**里的接单提示行：`keywordCardRegistry` 登记各群各工单最近一张带接单提示的卡片（播报卡/多人续接询问卡/超时重问询卡，IM 消息发送成功带 message_id 才登记），整卡重建后走飞书 `PATCH /im/v1/messages/:message_id`（bot.js 新增 `updateCardToChat`）改写；连续失败 5 次放弃该卡片（消息被删等），由每分钟对账刷新兜底；webhook 兜底发送的卡片无 message_id 不登记，只对新卡生效；
  - 指定负责人工单「公示即绑定」写补充负责人后仍可被本人确认（队列成员资格含绑定未确认态）；绑定后该单也在队列中，多单并存时本人在公示群按「接单N」或等它排到唯一时发「接单」均可；
  - 对账（`reconcileBroadcasts`）与接单/播报路径统一调 `refreshAcceptKeywordCards`，幂等（无变化零写操作）；
  - 面向多组别的工单按群独立编号、按群克隆队列成员（同单在不同群可带不同序号）；webhook-only 群不入队（收不到消息事件无接单链路）。
- 文案适配：超时重问询卡与组长私聊/指定负责人私信引导改述「按工单卡片提示发『接单N』，仅一张时发『接单』」；多人单续接询问卡、私聊无待办提示同步。
- 文档：README §2 播报分支、§3.3 新增「接单排队」小节、§十 注意事项（原「内存映射重启清空」描述改为实时推导口径）；DEVLOG 头部当前最新改 v57。
- 验证：离线桩测试全流程通过（老单独处=接单 → 新多人单入队后老卡 PATCH 成接单2 → 裸「接单」被拒提示 → 接单2 精确接老单 → 新单卡 PATCH 回落「接单」→ 裸「接单」命中唯一新单；第三单入队后多人续接询问卡 PATCH 成接单2）。部署验证记录随部署补记。

### v58 · 2026-09-08 · 67614fc · docs
**v57 部署验证结果记录（docs，无代码改动）**
- 部署：commit `2f464ea` 已推送 GitHub 并经 push.js 同步 NAS（/opt/ticket-bot），pm2 restart 后 ticket-bot online（重启计数 +1），NAS 侧 `git log -1` = 2f464ea 与本地一致；本地 .env 覆盖上传前已确认 `FEISHU_USE_LONG_CONNECTION=false`。
- 验证清单：`GET /api/health` 200（botName 爆米花机-对话型）；启动日志含「已配置为不使用长连接模式，跳过启动（需通过HTTP回调接收事件）」；`GET /api/tickets/unclosed-by-group` 返回 `{"result":{}}`（当前无未结单分桶数据，接口正常）；`POST /api/bot/test-nudge` 返回 success（checked 0）；重启后日志检索无 TypeError/ReferenceError（仅 2mb body 放宽前遗留的 PayloadTooLargeError 历史行，非本次引入）。
- 功能验证：离线桩测试（不落库/不发真实消息）覆盖全部排队场景——老单独处=「接单」→ 新多人单入队后老卡 PATCH 成「接单2」→ 裸「接单」被拒并提示序号范围 → 「接单2」精确接走老单 → 新单卡 PATCH 回落「接单」→ 裸「接单」命中唯一新单；第三单入队后多人续接询问卡（multi 类）PATCH 成「接单2」且重建含接单人（显示 open_id，与既有 POST 行为一致）。卡片 PATCH 接口为全链路唯一未真发验证的步骤，真实群内触发（多单并存自然场景）由线上事件驱动验证，次日看日志。

## 阶段十八 · 多人单卡单 + 指定负责人确认被误拒修复（2026-09-10）

### v59 · 2026-09-10 · d654c92 · fix
**两张工单卡死机械组群的根因修复：窗口截止写入字段类型不符（TextFieldConvFail）+ 指定负责人本人确认被当成「已确认」误拒**
- **事故现场**（机械组群 202609080001 / 202609100003，日志与源表核对）：① 202609080001 是多人单（「是否允许多人接单」=是），2026-09-08 17:18 白名单管理员接单、窗口开到 23:18:50，但源表「多人接单截止」恒为空 → 每分钟对账 `deadlineTs=0` 恒判「窗口未到期」→ **审批永不自通过**，该单在机械组群一直显示待接单（队列里还挂着「接单N」提示）——「卡工单」；② 202609100003 是指定负责人单，公示即绑定已写补充负责人=本人，白名单管理员在群内发「接单1」被回「你已确认过该工单」，本人确认被拒 → 节点永卡「负责人确认消息后通过」，审批同样不通过。
- **根因①：窗口截止写文本字段传了数字**。`writeMultiWindowDeadline` 把 `Date.now()+6h`（毫秒数字）写进「多人接单截止」——该字段由 `ensureWindowField` 以默认**文本型**创建（v57 上线时当场创建），飞书直接拒收：`更新记录失败: TextFieldConvFail (code: 1254060)`；错误被 try/catch 吞成一行 error 日志，接单流程继续走完（续接询问卡照发），**字段永远为空 → 对账永远判不出到期**。修复：写**带 +08:00 偏移的 ISO 文本**（`formatWindowDeadline`，飞书接受、表格可读），读取统一走 `parseWindowDeadline`（兼容 ISO 文本与历史数字串）。
- **根因②：v50 的「已确认过」守卫把「公示即绑定」当成已确认**。指定负责人单在绑定阶段就会写补充负责人=本人，守卫 `supNow.some(p=>p.id===userId) → 拒绝` 于是把本人挡在门外——而本人确认正是该链路的既定动作（绑定≠确认），**本人永远无法完成确认**（v45/v47 设计的闭环在这道守卫下从未跑通）。修复：`boundAssigneeSelf`（指定负责人单且补负含本人）放行确认；本人短窗（10 分钟）重复消息由内存 `assigneeConfirmState` 拦截（确认成功才登记；审批联动偶发失败时本人稍后可重试，见 LOGIC-MAP 已知限制 #11）。
- **配套加固（同类卡单不再可能）**：① 窗口截止写入改为返回成败，**写失败即降级为「接单即自动通过」**且不发续接询问（窗口是假的，绝不据此卡单）；② 对账遇多人单**截止缺失**（修复前存量/字段被清）不再静默跳过——重新计时回写 now+6h（写失败下轮重试），保证窗口最终闭合；③ 接单队列推导：多人单**截止已过即出队**（卡片不再提示接单，对账 1 分钟内关闭窗口）；④ 卡片重建的截止显示改走 `parseWindowDeadline`。
- **存量补救**：202609080001 的「多人接单截止」已按日志真实值回填 `2026-09-08T23:18:50+08:00`（脚本写入并回读校验），部署后首轮对账即自动通过审批并发「✅ 多人接单结束」卡；202609100003 需白名单管理员在机械组群重发一次「接单」（该单为指定负责人单，设计上只认本人确认，对账不代通过）——修复后该消息将正常完成确认、联动自动通过并回落队列序号（届时群内仅剩这一张，提示「接单」）。
- 验证：离线桩测试（stub 全部外部依赖，不落库/不发真实消息）20/20 通过——多人单到期对账→自动通过+结束卡；截止缺失→重新计时不误通过；窗口未到期→零动作；指定负责人本人确认→放行且联动角色「负责人」；短窗重复→拒绝；多人单接单→截止以 ISO 文本落库、不即时通过；写入失败→降级自动通过、不发续接询问；截止已过单出队/缺截止单在队。
- 文档：README §3 接单确认（多人单分支与写入失败降级、绑定≠已确认）、§3.3（到期出队）、配置表（窗口字段为文本型 ISO）；ticket-pm/LOGIC-MAP.md §1.3（群内链路守卫、多人单分支写入口径与自愈）、已知限制 #11。

### v60 · 2026-09-10 · 1884ea0 · docs
**v59 部署验证结果记录（docs，无代码改动）**
- 部署：commit `d654c92` 已推送 GitHub 并经 push.js 同步 NAS（/opt/ticket-bot），pm2 restart 后 ticket-bot online（重启计数 22）；本地 `.env` 原样覆盖上传（本次无新配置项，`.env.example` 不动）。
- 验证清单：`GET /api/health` 200；启动日志含「已配置为不使用长连接模式，跳过启动」；err.log 尾部 `fetch failed` 计数跨一个完整 cron 轮次不变（67→67，重启窗口期/历史遗留），每分钟对账持续输出「扫描 38 条」，无 TypeError/ReferenceError。
- 功能验证（真实链路，非桩）：`POST /api/bot/reconcile` 返回 `multiClosed:1`；日志依次为 `[审批联动] 缓存缺失，已按申请编号反查定位 1 个待审任务: 202609080001` → `已自动通过审批: 202609080001（1/1 个任务）` → `[多人接单] 窗口结束，审批已自动通过并通知 1 个群: recvuCqiEqUSFJ`。审批实例核对：触发节点任务 `APPROVED`，流程已推进并生成「回执单：是否结单」任务（待发起人处理）——**卡单闭环，机械组群不再挂着这张待接单工单**。源表「审批节点」字段暂未回写属预期（由审批侧同步自动化刷新，v56 已记录；工单侧无依赖，字段落回前每 10 分钟节流重试一次自动通过、失败只留一行日志，不会重复发结束通告）。
- 端到端写入验证：修复版写入格式（ISO 文本）经飞书接受（回填 202609080001 截止时实测「写入成功→回读一致」）；离线回归测试 `scripts/stub-test-multi-accept.js` 随本批入库（20 项断言全过，全外部依赖走桩，不落库不发真实消息）。
- 存量待办（非本仓代码问题）：**202609100003（recvuPkRyCNJCb，指定负责人=白名单管理员）** 需本人在机械组群（或私聊机器人）重发一次「接单」——该单设计上只认本人确认（对账不代通过指定负责人单），修复后该确认将正常完成：写补充负责人（已在）→ 看板状态推进 → 群内回执 → 自动通过「负责人确认消息后通过」→ 流程推进。届时该群待接单工单清零，卡片提示回落「接单」。

### v61 · 2026-09-12 · f6f73e2 · fix

**多人接单批次收尾：出站调用全量超时加固 + 审批人解析失败不缓存自愈 + 接单队列免二次拉表 + 静默积压挪址 + 历史文档隐私清洗**

- 出站网络加固：`src/feishu/bot.js` 群 webhook 发送与 `src/feishu/client.js` requestAPI 均加 15s `AbortSignal.timeout`——无超时的 fetch 挂起会拖住播报链路、拖死每分钟对账与全部 cron；client.js 对非 JSON 响应抛可读错误（带 HTTP 状态码与 path），不再吞成晦涩的 parse 报错。
- 审批人解析自愈：`approvalLinkService.resolveToOpenId` 失败的 null 不再写缓存——缓存住会让该审批人的任务在事件缓存与反查兜底两路同时 fail-closed，对账补偿也无法自愈（工单卡在触发节点直到重启）；瞬时失败留给下个审批事件/下轮对账重试。顺手修正「未激活」日志与注释里 user_id/open_id 的误标。
- 接单队列免二次拉表：`broadcastTicket` 首轮拉取的全量记录复用给 `computeAcceptQueues` 与 `refreshAcceptKeywordCards`（两函数签名 v57/v59 已支持 records 入参），同一次播报链路少一次全表拉取；首轮失败时传 null 走函数内现查，行为不回退。
- 静默积压挪址：quietHours 积压文件支持 `QUIET_BACKLOG_FILE` 挪到项目目录外（push.js SFTP 兜底部署 rm -rf 清空目录会丢积压；本仓主路径 git fetch + reset --hard 保留未跟踪文件，兜底路径才受影响），本仓 `.env` 配置 `/home/qianli/ticket-bot-data/quiet-backlog.json`，quietHours 加载时自动建目录；`.env.example` 同步补键（补齐「新增配置同步改 example」欠账）。
- 隐私清洗：DEVLOG 历史条目（v44/v45/v59 等）与 `scripts/stub-test-multi-accept.js` 中的真实姓名改为通用称谓（白名单管理员/测试员甲乙）——.env 不进 git，但文档与测试脚本会。
- 已知待办：`GET /api/tickets/policy` 定制窗口仍未在本批落地（顶层 registry 注记同步改写为待后续批次）。

### v62 · 2026-09-12 · b9178d5 · feat

**动态广场事件流接入（工单播报/接单/结单/审批自动通过 → 机器人项目看板）**

- 新增 `src/services/plaza.js`：关键业务事件写机器人项目看板「动态广场」表（`config.plaza` 默认表已内置，`PLAZA_BITABLE_TABLE_ID` 可覆盖）；失败仅 warn 绝不影响主流程，未配置表整体静默。
- 插桩三点：`broadcastTicket` 成功播报（工单播报）、接单确认（工单接单，含角色）、对账多人单窗口到期自动通过（工单结单）与补通过（审批自动通过）。
- 测试：`scripts/stub-test-multi-accept.js` 补 `PLAZA_BITABLE_TABLE_ID=''` 隔离（防测试污染生产表），20/20 回归全过。

### v63 · 2026-09-12 · 随本提交落地 · docs

**全量 debug 批：文档补齐 + DEVLOG 哈希回填（无代码改动）**

- README §6 晚间静默变量表补 `QUIET_BACKLOG_FILE` 键（v61 落地时漏记模板行）。
- DEVLOG 哈希回填：v54（2df1b23）/ v55（89d23c8）/ v56（b8bad0f）/ v57（2f464ea）/ v58（67614fc）/ v59（d654c92）/ v60（1884ea0）/ v61（f6f73e2）/ v62（b9178d5）；头部「当前最新」指针 v60 → v63。
